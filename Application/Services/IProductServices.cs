namespace GetTheRepsWeb.Application.Services
{
    public interface IProductServices
    {
        // inspect the use case tiles 

        // im thinking that we should use a exception handling... but what kind? this is certainly a testable
        // piece of code. so we need to be able to simulate the data not being found, the query being invalid.... hmmm what else?

        // call the repository and tell it what we need, this obviously needs to be async....
    }
}
